import React from "react";

export const Hook = () => {

    class Groups extends React.Component {
        render() {
            return (
                <div>
                   <p>Grupos</p>
                </div>
            );
        }
    }
    return (<Groups />);
};

export default Hook;